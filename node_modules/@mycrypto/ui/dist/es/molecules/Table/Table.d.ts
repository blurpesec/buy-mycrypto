import React, { ReactNode } from 'react';
import { StyledComponentClass } from 'styled-components';
import Theme from '../../Theme';
export interface TableGroup {
    title: string;
    entries: ReactNode[][];
    offset?: number;
}
export interface TableConfig {
    sortableColumn?: string | null;
    hiddenHeadings?: string[];
    reversedColumns?: string[];
    sortFunction?(a: any, b: any): number;
}
export interface TableContent {
    body: ReactNode[][];
    groups?: TableGroup[];
}
export interface TableData extends TableContent {
    head: string[];
    overlay?: ReactNode;
    overlayRows?: number[];
    config?: TableConfig;
}
export declare enum ColumnDirections {
    Forward = 0,
    Reverse = 1
}
export declare const defaultColumnSort: (a: any, b: any) => number;
export declare const getSortedRows: (head: any[], body: React.ReactNode[][], config: TableConfig, sortedColumnDirection: ColumnDirections) => React.ReactNode[][];
export declare const Table: StyledComponentClass<TableData, Theme, Pick<TableData, "body" | "head" | "overlay" | "overlayRows" | "config" | "groups"> & {
    theme?: Theme | undefined;
}>;
export default Table;
//# sourceMappingURL=Table.d.ts.map