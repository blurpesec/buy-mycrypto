import React, { Component } from 'react';
import { StackedCardData, TableConfig, TableData } from '../../molecules';
export declare enum CollapsibleTableModes {
    Mobile = 0,
    Desktop = 1
}
export interface CollapsibleTableConfig extends TableConfig {
    primaryColumn: string;
    iconColumns?: string[];
}
export interface CollapsibleTableData extends TableData {
    config: CollapsibleTableConfig;
}
interface CollapsedGroups {
    [title: string]: boolean;
}
interface Props extends CollapsibleTableData {
    breakpoint: number;
}
interface State {
    mode: CollapsibleTableModes;
    collapsedGroups: CollapsedGroups;
}
export declare const transformRowToCards: (row: React.ReactNode[], head: string[], primaryColumnIndex: number, iconColumns?: string[]) => StackedCardData;
export declare const transformTableToCards: ({ head, body, groups, config }: CollapsibleTableData, collapsedGroups?: CollapsedGroups) => (string | StackedCardData)[];
export declare const screenIsMobileSized: (breakpoint: number) => boolean;
export declare class CollapsibleTable extends Component<Props, State> {
    static defaultProps: {
        head: never[];
        body: never[];
        groups: never[];
        config: {};
        breakpoint: number;
    };
    constructor(props: Props);
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element | JSX.Element[];
    private readonly checkWindowSize;
    private readonly toggleCollapseGroup;
}
export default CollapsibleTable;
//# sourceMappingURL=CollapsibleTable.d.ts.map